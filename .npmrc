# ts-jest@20 peer-requires typescript@2.x while this project builds with
# typescript@3. The combination works, but npm 7+ refuses to resolve it.
# Remove this once jest/ts-jest are upgraded.
legacy-peer-deps=true
